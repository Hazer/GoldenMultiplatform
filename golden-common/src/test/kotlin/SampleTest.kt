@file:Suppress("FunctionName")

package io.vithor.archi.common

import kotlin.test.Test
import kotlin.test.assertTrue

@Test
fun `Just a sample test`() {
    assertTrue(true, message = "Just showing we can test it.")
}
